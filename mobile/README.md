# mobile (reserved)

Not built yet. When native iOS/Android + reliable push is needed, wrap the
frontend PWA with Capacitor here rather than building a separate app — the web
codebase is reused. Alternative: React Native sharing packages/shared.
