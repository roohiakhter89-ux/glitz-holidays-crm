# packages/shared (reserved)

Home for TypeScript types/interfaces shared across backend, frontend, and mobile
(e.g. Lead, LeadStatus, Vendor DTOs) so the API contract is defined once.
Wired up only when it earns its place — no workspace tooling yet.
